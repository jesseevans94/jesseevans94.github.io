let x

// array literal
const numbers = [12, 45, 33, 29, 29, 102]

const mixed = [12, 'Hello', true, null]

// get the vLUE BY INDEX
x = numbers[0]

console.log(x)

x = numbers [0] + numbers [3] = 12 + 29

// array constructor
const fruits = new Array('apple', "grape", "Orange")

x = numbers.length

fruits[2] = 'pear'

x = fruits 

fruits[fruits.length] = 'blueberry'


const arr = [28, 38, 44, 29, 109]

// push value to end of array
arr.push(100)

// takes last value out of array
arr.pop()

// unshift- adds a value to beginning
arr.unshift(99)

// remove first value

arr.shift()

// reverse array
arr.reverse()

// check to see if something is in array
arr.includes(445)

// check the index of an array
x = arr.indexOf(28)

// slice returns selected elements in an array as a new array
x = arr.slice(1,4)


// splce will remove the values and mutate the original array
x= arr.splice(1, 4).reverse()






console.log(x)